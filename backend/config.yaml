server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "tiaozao"
  sslmode: "disable"
  timezone: "Asia/Shanghai"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "tiaozao-jwt-secret-key-change-in-production"
  expire_hours: 72

sms:
  # Mock mode: when true, verification codes are logged instead of sent via SMS
  mock_mode: true
  # In mock mode, this code is always accepted
  mock_code: "123456"
  expire_minutes: 5
  # For production: Aliyun SMS config
  access_key_id: ""
  access_key_secret: ""
  sign_name: "跳蚤市场"
  template_code: "SMS_XXXXX"
